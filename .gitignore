node_modules/
*.log
.DS_Store

# секреты воркера
pravofin/worker/.env
worker/.env
.env

# номер версии ресурсов ведётся скриптом bump-version.py

# Готовая настройка машины: содержит ключи из worker/.env
pravofin/worker/node/cloud-init.ready.yaml
worker/node/cloud-init.ready.yaml

# Однострочная команда с ключами для Cloud Shell
worker/node/oneliner.txt
pravofin/worker/node/oneliner.txt
worker/node/chunks.txt
pravofin/worker/node/chunks.txt

# Локальная база разработки. Рабочая живёт на сервере, в /opt/pravofin/data,
# и из репозитория не берётся: здесь она появляется только когда сервер
# запускают на своей машине для проверки.
worker/data/
