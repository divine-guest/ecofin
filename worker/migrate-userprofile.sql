-- Профиль пользователя переезжает из браузера на сервер.
--
-- Ответы на три вопроса при знакомстве (кто вы, сфера, что важнее)
-- сохранялись только в localStorage. Кабинет читал их из данных,
-- которые приходят с сервера, — то есть не читал никогда: блок
-- «Рекомендуем для вас» не показался ни одному человеку с момента
-- появления. И при входе с телефона ответы терялись целиком.
--
-- Здесь же живут ответы мастера налогового календаря: режим и
-- наличие работников. Они нужны и на сервере — по ним подбираются
-- сроки, и в боте.
ALTER TABLE users ADD COLUMN profile TEXT DEFAULT '';
