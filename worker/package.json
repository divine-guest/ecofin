{
  "name": "pravofin-ai-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put AI_API_KEY",
    "tail": "wrangler tail"
  },
  "devDependencies": { "wrangler": "^4.20.0" }
}
