-- Еженедельная сводка.
-- Запускается один раз; повторный запуск вернёт «duplicate column» — это нормально.
--
-- digest_week — номер недели, за которую сводка уже отправлена. Без него
-- крон, который срабатывает каждый час, разослал бы её по разу в час
-- в течение всего понедельника.
--
-- digest_off — человек отключил сводку. Отдельным полем, а не удалением
-- строки: настройку нужно помнить.

ALTER TABLE users ADD COLUMN digest_week INTEGER;
ALTER TABLE users ADD COLUMN digest_off INTEGER NOT NULL DEFAULT 0;
