-- Профиль компетенций на сервере.
-- Запускается один раз; повторный запуск вернёт «duplicate column» — это нормально.
--
-- Зачем: прогресс обучения хранился в localStorage и обнулялся при
-- смене устройства или чистке кэша. Раздел, который показывает рост
-- за месяцы и сбрасывается от одной кнопки, доверия не вызывает.
--
-- Хранится json вида {"tax":40,"contracts":15,...} — отдельная таблица
-- избыточна: значений пять, читаются и пишутся всегда целиком.

ALTER TABLE users ADD COLUMN competencies TEXT;
