-- Блокнот в кабинете.
-- Запускается один раз; повторный запуск вернёт «duplicate column» — это нормально.
--
-- Зачем: людям нужно место, чтобы записать «спросить бухгалтера про
-- патент» или реквизиты контрагента. Сейчас они пишут это себе в
-- мессенджер и теряют.
--
-- Одно текстовое поле, а не таблица записей: это блокнот, а не список
-- задач. Список задач уже есть — это напоминания о сроках.

ALTER TABLE users ADD COLUMN notes TEXT;
ALTER TABLE users ADD COLUMN notes_at INTEGER;
