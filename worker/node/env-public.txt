# Настройки, которые не секрет.
#
# Секреты — ключи, токены, пароли — живут только на самой машине, в файле
# /opt/pravofin/env, и в репозиторий не попадают никогда. Но не всё в
# настройках секретно: адрес сайта знает каждый посетитель, список
# разрешённых источников — тоже не тайна.
#
# Такие строки лежат здесь. Установщик накладывает их поверх машинных:
# что написано тут, то и побеждает. Значит поменять адрес сайта можно
# обычной выкладкой, не пересоздавая машину и не трогая секреты.
#
# Формат обычный: КЛЮЧ=значение, по одной строке. Пустые строки и строки,
# начинающиеся с решётки, пропускаются.
#
# ВАЖНО: сюда нельзя класть ключи и токены. Репозиторий открыт, и всё,
# что здесь написано, видно всему интернету.

# Адрес, который сервер подставляет в письма, ссылки бота и возврат после
# оплаты. Должен вести туда, где сайт реально открывается: пока это сам
# сервер по временному имени, потом будет купленный домен.
SITE_URL=https://ecofin26.ru/

# Кому разрешено обращаться к API с чужого адреса.
#
# Свой адрес разрешён всегда и без списка — сайт обращается сам к себе.
# Здесь только посторонние: GitHub Pages, где пока лежит прежняя версия
# сайта, и временное имя nip.io.
#
# Временное имя оставлено намеренно: пока новый домен расходится по DNS,
# часть людей ещё приходит по старому адресу, и обрывать им доступ на
# середине переезда незачем. Убрать через неделю после переезда.
ALLOWED_ORIGINS=https://divine-guest.github.io,https://84-201-164-112.nip.io,https://ecofin26.ru,https://www.ecofin26.ru
